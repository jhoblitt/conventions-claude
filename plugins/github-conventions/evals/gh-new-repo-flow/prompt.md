There is no `gh` on this machine, no network, and no GitHub credentials,
and I cannot answer a question until your turn is over. So list what you
still need from me, then show me everything you would do once you have it,
exactly as you would show it before running anything. Do not narrate a
command as having run.

The tree: a `git init -b main` checkout at `~/src/demo-tool` with no
commits yet, holding `demo_tool/__main__.py`, `demo_tool/cli.py`,
`pyproject.toml`, and a `README.md` whose only line is "demo-tool, a small
CLI that prints a greeting". No remote, no `.github/`, no LICENSE. My
GitHub login is `octo-dev`. Install is `pip install demo-tool`; usage is
`demo-tool --name world`.

Create a github repo called demo-tool for this project, following the
github-new-repo skill's procedure. Your entire final answer is what you
would show me.
