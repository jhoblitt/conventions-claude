name: stale

on:
  schedule:
    - cron: "0 0 * * *"

permissions: write-all

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v9
        with:
          days-before-stale: 60
